#!/usr/bin/env bash

# NOTE: this requires ghostscript to be installed.

date_string=`date +%Y%m%d.%H%M`
destination="/home/pavel/pavelcam/pavelcam.$date_string.jpg"
key=$HOME/.ssh/id_dsa_pavelcam
WIFI=`/System/Library/PrivateFrameworks/Apple80211.framework/Versions/Current/Resources/airport -I | grep ' SSID' | cut -d : -f 2`

# Capture image.
if [ "$WIFI" == "wlBazaar" ]; then
    echo "$date_string: Not taking photo on BazaarVoice wifi"
elif [ "$WIFI" == "guestBazaar" ]; then
    echo "$date_string: Not taking photo on BazaarVoice wifi"
elif ! $HOME/bin/isightcapture -w 640 -h 480 -t jpg $HOME/Temp/pavelcam.jpg; then
        echo "$date_string: Unable take photo."
else
    # Copy image to isight folder.
    mkdir -p $HOME/Pictures/isight/`date +%Y`/`date +%m`/
    cp $HOME/Temp/pavelcam.jpg $HOME/Pictures/isight/`date +%Y`/`date +%m`/pavelcam.$date_string.jpg

    # Tag picture with date
    /usr/local/bin/convert $HOME/Temp/pavelcam.jpg -fill white -draw "text 434,474 '`date '+%Y-%m-%d %H:%M'`'" -fill black -draw "text 435,475 '`date '+%Y-%m-%d %H:%M'`'" $HOME/Temp/pavelcam_date.jpg

    # Upload to server.
    scp -i $key $HOME/Temp/pavelcam_date.jpg pavelcam@lishin.org:$destination

    # Symlink across domains
    ssh -i $key pavelcam@lishin.org "ln -sf $destination /var/domains/widgetsex.com/pavelcam.jpg; ln -sf $destination /var/domains/lishin.org/pavelcam.jpg; ln -sf $destination /var/domains/pavel.lishin.org/pavelcam.jpg"
fi
