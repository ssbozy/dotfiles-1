# As far as I can tell, the only thing that uses this
# is ubuntu's lightdm-session garbage, and it hates bash.
# And therefore, doesn't let me log in.
# So fuck it.

# Source the bash rc file
# if [ -r "${HOME}/.bashrc" ]; then
#     . "${HOME}/.bashrc"
# fi
