#/usr/bin/env bashrc

# Launch tinyur desktop screenshot monitor
if ! ps ax | egrep tinyu[r] > /dev/null; then
    /Users/pavel/projects/tinyur/tinyur.py &
fi

# Launch synergy
if ! ps ax | egrep synergy[c] > /dev/null; then
    /Users/pavel/synergy_moskva
fi

# Give me my nice bash prompt if I kill synergy
/usr/bin/env bash

